services:
  - type: web
    name: erp-demand-forecast-api
    env: python
    rootDir: backend
    # Use Python 3.11 on Render to ensure prebuilt wheels for pydantic-core are available.
    pythonVersion: 3.11
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: MODEL_PATH
        value: models/xgboost_model.pkl
      - key: DATASET_DIR
        value: datasets
      - key: ALLOWED_ORIGINS
        sync: false
      - key: LOG_LEVEL
        value: INFO
